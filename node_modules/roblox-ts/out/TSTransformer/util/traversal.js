"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getModuleAncestor = exports.getAncestor = exports.skipUpwards = exports.skipDownwards = exports.isAncestorOf = void 0;
const byots_1 = __importDefault(require("byots"));
function isAncestorOf(ancestor, node) {
    do {
        if (ancestor === node) {
            return true;
        }
        node = node.parent;
    } while (node);
    return false;
}
exports.isAncestorOf = isAncestorOf;
function skipDownwards(node) {
    while (byots_1.default.isNonNullExpression(node) || byots_1.default.isParenthesizedExpression(node) || byots_1.default.isAsExpression(node)) {
        node = node.expression;
    }
    return node;
}
exports.skipDownwards = skipDownwards;
function skipUpwards(node) {
    let parent = node.parent;
    while (parent &&
        (byots_1.default.isNonNullExpression(parent) || byots_1.default.isParenthesizedExpression(parent) || byots_1.default.isAsExpression(parent))) {
        node = parent;
        parent = node.parent;
    }
    return node;
}
exports.skipUpwards = skipUpwards;
function getAncestor(node, check) {
    let current = node;
    while (current && !check(current)) {
        current = current.parent;
    }
    return current;
}
exports.getAncestor = getAncestor;
function isSourceFileOrModuleDeclaration(node) {
    return byots_1.default.isSourceFile(node) || byots_1.default.isModuleDeclaration(node);
}
function getModuleAncestor(node) {
    return getAncestor(node, isSourceFileOrModuleDeclaration);
}
exports.getModuleAncestor = getModuleAncestor;
//# sourceMappingURL=traversal.js.map